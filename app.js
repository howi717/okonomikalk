const kr=new Intl.NumberFormat('no-NO',{style:'currency',currency:'NOK',maximumFractionDigits:0});
const money=v=>kr.format(Math.round(Number(v)||0));
const qs=id=>document.getElementById(id);
function yearlyBillableHours(hoursPerWeek,vacationDays){return Math.max(0,52-(vacationDays/5))*hoursPerWeek}
function startTimesprisQuiz(){qs('timesprisForm').addEventListener('submit',e=>{e.preventDefault();const desired=+qs('desiredIncome').value||0,costs=+qs('costs').value||0,hours=+qs('hoursPerWeek').value||0,vac=+qs('vacationDays').value||0,buf=(+qs('bufferPercent').value||0)/100;const turnover=desired+costs,total=turnover*(1+buf),bill=yearlyBillableHours(hours,vac),rate=bill>0?total/bill:0;qs('timesprisResult').innerHTML=`<p class="eyebrow">Resultat</p><h2>Anbefalt timespris eks. mva</h2><span class="result-number result-good">${money(rate)}</span><ul class="result-list"><li>Ønsket inntekt: ${money(desired)}</li><li>Driftskostnader: ${money(costs)}</li><li>Omsetning før buffer: ${money(turnover)}</li><li>Omsetning med buffer: ${money(total)}</li><li>Fakturerbare timer per år: ${Math.round(bill)}</li></ul><div class="cta-box"><h3>Vil du se full beregning?</h3><p>Bruk Økonomikalk Premium for timespris, skatt, fradrag og rapport.</p><a class="btn primary" href="https://okonomikalk.no">Åpne Økonomikalk</a></div>`;qs('timesprisResult').classList.add('active')})}
function startSkattebufferQuiz(){qs('skattebufferForm').addEventListener('submit',e=>{e.preventDefault();const revenue=+qs('revenue').value||0,costs=+qs('costs').value||0,result=Math.max(0,revenue-costs);let pct=30;if(result>700000)pct=38;else if(result>450000)pct=34;else if(result<250000)pct=25;const yearly=revenue*pct/100,monthly=yearly/12;qs('skattebufferResult').innerHTML=`<p class="eyebrow">Resultat</p><h2>Du bør vurdere å sette av</h2><span class="result-number result-good">${pct} %</span><ul class="result-list"><li>Ca. skattebuffer per år: ${money(yearly)}</li><li>Ca. skattebuffer per måned: ${money(monthly)}</li><li>Estimert næringsresultat før skatt: ${money(result)}</li></ul><div class="cta-box"><h3>Dette er bare et grovt estimat</h3><p>Få en mer detaljert beregning i Økonomikalk Premium.</p><a class="btn primary" href="https://okonomikalk.no">Se full beregning</a></div>`;qs('skattebufferResult').classList.add('active')})}
function startBudgetQuiz(){qs('budgetForm').addEventListener('submit',e=>{e.preventDefault();const inc=+qs('monthlyIncome').value||0,fixed=+qs('fixedCosts').value||0,variable=+qs('variableCosts').value||0,savings=+qs('savings').value||0,total=fixed+variable+savings,left=inc-total,ratio=inc>0?total/inc:0;let cls='result-good',status='Budsjettet ser sunt ut';if(left<0){cls='result-danger';status='Budsjettet går i minus'}else if(ratio>.9){cls='result-warn';status='Budsjettet er ganske stramt'}qs('budgetResult').innerHTML=`<p class="eyebrow">Resultat</p><h2>${status}</h2><span class="result-number ${cls}">${money(left)}</span><ul class="result-list"><li>Inntekt per måned: ${money(inc)}</li><li>Utgifter og sparing per måned: ${money(total)}</li><li>Resultat per år: ${money(left*12)}</li></ul><div class="cta-box"><h3>Vil du lage et mer detaljert budsjett?</h3><p>Bruk budsjettverktøyet i Økonomikalk.</p><a class="btn primary" href="https://okonomikalk.no">Lag fullstendig budsjett</a></div>`;qs('budgetResult').classList.add('active')})}
function startLoanQuiz(){qs('loanForm').addEventListener('submit',e=>{e.preventDefault();const amount=+qs('loanAmount').value||0,years=+qs('years').value||0,rate=(+qs('rate').value||0)/100,income=+qs('income').value||0,months=years*12,mr=rate/12,monthly=mr===0?amount/months:amount*mr/(1-Math.pow(1+mr,-months)),burden=income>0?monthly/income:0;let cls='result-good',status='Lånet virker håndterbart';if(burden>.4){cls='result-danger';status='Lånet virker tungt for økonomien'}else if(burden>.28){cls='result-warn';status='Lånet tar en stor del av inntekten'}qs('loanResult').innerHTML=`<p class="eyebrow">Resultat</p><h2>${status}</h2><span class="result-number ${cls}">${money(monthly)}</span><ul class="result-list"><li>Estimert månedlig lånekostnad</li><li>Andel av månedlig inntekt: ${Math.round(burden*100)} %</li><li>Total tilbakebetaling: ${money(monthly*months)}</li></ul><div class="cta-box"><h3>Vil du sammenligne lån?</h3><p>Bruk lånekalkulatoren i Økonomikalk.</p><a class="btn primary" href="https://okonomikalk.no">Åpne lånekalkulator</a></div>`;qs('loanResult').classList.add('active')})}
function startOptionQuiz(config){let current=0,score=0;const questions=[...document.querySelectorAll('.question')],progress=qs('progressBar'),result=qs('result');function show(i){questions.forEach((q,n)=>q.classList.toggle('active',n===i));progress.style.width=`${(i/questions.length)*100}%`}document.querySelectorAll('[data-score]').forEach(btn=>btn.addEventListener('click',()=>{score+=Number(btn.dataset.score||0);current++;if(current>=questions.length){progress.style.width='100%';questions.forEach(q=>q.classList.remove('active'));config.render(score,result);result.classList.add('active');return}show(current)}));show(0)}
