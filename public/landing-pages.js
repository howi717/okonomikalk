
const moneyFormatter=new Intl.NumberFormat("no-NO",{style:"currency",currency:"NOK",maximumFractionDigits:0});
const money=v=>moneyFormatter.format(Math.round(Number(v)||0));
const nv=id=>Number(document.getElementById(id)?.value||0);
function setupStepper(render){let current=0;const steps=[...document.querySelectorAll(".step")],progress=document.getElementById("progressBar"),result=document.getElementById("resultCard");function show(i){steps.forEach((s,n)=>s.classList.toggle("active",n===i));progress.style.width=`${(i/steps.length)*100}%`}document.querySelectorAll("[data-next]").forEach(b=>b.addEventListener("click",()=>{current++;if(current>=steps.length){progress.style.width="100%";steps.forEach(s=>s.classList.remove("active"));render();result.classList.add("active");return}show(current)}));document.querySelectorAll("[data-back]").forEach(b=>b.addEventListener("click",()=>{current=Math.max(0,current-1);show(current)}));show(0)}
function attachEdit(render){document.querySelectorAll("[data-sync]").forEach(input=>{["input","change"].forEach(evt=>input.addEventListener(evt,()=>{const t=document.getElementById(input.dataset.sync);if(t)t.value=input.value}))});document.querySelectorAll("[data-update-result]").forEach(b=>b.addEventListener("click",render));document.querySelectorAll("[data-restart]").forEach(b=>b.addEventListener("click",()=>location.reload()))}
function cta(text,button){return `<div class="cta-box"><h3>${text}</h3><p class="help">Gå videre til Økonomikalk for full kalkulator, utskrift, rapport og flere detaljer.</p><a class="btn primary" href="index.html#kalkulatorer">${button}</a></div><div class="actions"><button class="btn ghost" type="button" data-restart>Start på nytt</button></div>`}
function initTimespris(){function render(){const desired=nv("desiredIncome"),costs=nv("costs"),hours=nv("hoursPerWeek"),vac=nv("vacationDays"),buffer=nv("bufferPercent")/100,weeks=Math.max(0,52-vac/5),bill=weeks*hours,turnover=desired+costs,total=turnover*(1+buffer),hourly=bill>0?total/bill:0;document.getElementById("resultContent").innerHTML=`<p class="eyebrow">Resultat</p><h2>Anbefalt timespris eks. mva</h2><span class="result-number">${money(hourly)}</span><div class="summary-grid"><div class="summary-item"><span>Ønsket inntekt</span><strong>${money(desired)}</strong></div><div class="summary-item"><span>Driftskostnader</span><strong>${money(costs)}</strong></div><div class="summary-item"><span>Omsetning før buffer</span><strong>${money(turnover)}</strong></div><div class="summary-item"><span>Omsetning med buffer</span><strong>${money(total)}</strong></div><div class="summary-item"><span>Fakturerbare timer per år</span><strong>${Math.round(bill)}</strong></div><div class="summary-item"><span>Buffer</span><strong>${Math.round(buffer*100)} %</strong></div></div><div class="edit-panel"><h3>Juster tallene</h3><div class="edit-grid"><label>Ønsket inntekt<input type="number" value="${desired}" data-sync="desiredIncome"></label><label>Driftskostnader<input type="number" value="${costs}" data-sync="costs"></label><label>Timer per uke<input type="number" value="${hours}" data-sync="hoursPerWeek"></label><label>Feriedager<input type="number" value="${vac}" data-sync="vacationDays"></label></div><div class="actions"><button class="btn secondary" type="button" data-update-result>Oppdater resultat</button></div></div>${cta("Se full timespris- og skatteberegning i Økonomikalk","Åpne Økonomikalk")}`;attachEdit(render)}setupStepper(render)}
function initSkattebuffer(){function render(){const revenue=nv("revenue"),costs=nv("costs"),res=Math.max(0,revenue-costs);let p=30;if(res>700000)p=38;else if(res>450000)p=34;else if(res<250000)p=25;const yearly=revenue*p/100,monthly=yearly/12;document.getElementById("resultContent").innerHTML=`<p class="eyebrow">Resultat</p><h2>Du bør vurdere å sette av</h2><span class="result-number">${p} %</span><div class="summary-grid"><div class="summary-item"><span>Skattebuffer per år</span><strong>${money(yearly)}</strong></div><div class="summary-item"><span>Skattebuffer per måned</span><strong>${money(monthly)}</strong></div><div class="summary-item"><span>Fakturert eks. mva</span><strong>${money(revenue)}</strong></div><div class="summary-item"><span>Estimert næringsresultat</span><strong>${money(res)}</strong></div></div><div class="edit-panel"><h3>Juster tallene</h3><div class="edit-grid"><label>Fakturert inntekt<input type="number" value="${revenue}" data-sync="revenue"></label><label>Kostnader<input type="number" value="${costs}" data-sync="costs"></label></div><div class="actions"><button class="btn secondary" type="button" data-update-result>Oppdater resultat</button></div></div>${cta("Få detaljert skatteberegning, fradrag og rapport i Økonomikalk","Åpne Økonomikalk")}`;attachEdit(render)}setupStepper(render)}
function initBudget(){function render(){const income=nv("income"),housing=nv("housing"),food=nv("food"),other=nv("other"),savings=nv("savings"),total=housing+food+other+savings,left=income-total,cls=left>=0?"":" result-danger";document.getElementById("resultContent").innerHTML=`<p class="eyebrow">Resultat</p><h2>${left>=0?"Budsjettet går i pluss":"Budsjettet går i minus"}</h2><span class="result-number${cls}">${money(left)}</span><div class="summary-grid"><div class="summary-item"><span>Inntekt per måned</span><strong>${money(income)}</strong></div><div class="summary-item"><span>Utgifter og sparing</span><strong>${money(total)}</strong></div><div class="summary-item"><span>Resultat per måned</span><strong>${money(left)}</strong></div><div class="summary-item"><span>Resultat per år</span><strong>${money(left*12)}</strong></div></div><div class="edit-panel"><h3>Juster tallene</h3><div class="edit-grid"><label>Utbetalt inntekt<input type="number" value="${income}" data-sync="income"></label><label>Bolig/lån/husleie<input type="number" value="${housing}" data-sync="housing"></label><label>Mat og dagligvarer<input type="number" value="${food}" data-sync="food"></label><label>Andre utgifter<input type="number" value="${other}" data-sync="other"></label><label>Sparing<input type="number" value="${savings}" data-sync="savings"></label></div><div class="actions"><button class="btn secondary" type="button" data-update-result>Oppdater resultat</button></div></div>${cta("Lag et mer detaljert privatbudsjett i Økonomikalk","Åpne budsjettverktøy")}`;attachEdit(render)}setupStepper(render)}
function initLoan(){function render(){const amount=nv("loanAmount"),years=nv("years"),rate=nv("rate")/100,income=nv("income"),months=years*12,mr=rate/12,monthly=mr===0?amount/months:amount*mr/(1-Math.pow(1+mr,-months)),burden=income>0?monthly/income:0,total=monthly*months;let title="Lånet virker håndterbart",cls="";if(burden>.4){title="Lånet virker tungt for økonomien";cls=" result-danger"}else if(burden>.28){title="Lånet tar en stor del av inntekten";cls=" result-warn"}document.getElementById("resultContent").innerHTML=`<p class="eyebrow">Resultat</p><h2>${title}</h2><span class="result-number${cls}">${money(monthly)}</span><div class="summary-grid"><div class="summary-item"><span>Månedlig kostnad</span><strong>${money(monthly)}</strong></div><div class="summary-item"><span>Andel av inntekt</span><strong>${Math.round(burden*100)} %</strong></div><div class="summary-item"><span>Total tilbakebetaling</span><strong>${money(total)}</strong></div><div class="summary-item"><span>Total rentekostnad</span><strong>${money(total-amount)}</strong></div></div><div class="edit-panel"><h3>Juster tallene</h3><div class="edit-grid"><label>Lånebeløp<input type="number" value="${amount}" data-sync="loanAmount"></label><label>Løpetid i år<input type="number" value="${years}" data-sync="years"></label><label>Rente %<input type="number" value="${nv("rate")}" data-sync="rate"></label><label>Utbetalt inntekt per måned<input type="number" value="${income}" data-sync="income"></label></div><div class="actions"><button class="btn secondary" type="button" data-update-result>Oppdater resultat</button></div></div>${cta("Test flere scenarioer i lånekalkulatoren","Åpne Økonomikalk")}`;attachEdit(render)}setupStepper(render)}
function initFradrag(){function render(){const ids=["software","equipment","travel","marketing","insurance"],score=ids.map(id=>document.getElementById(id).value).filter(v=>v==="yes").length;let title="Sjekk fradragene dine nærmere",cls="";if(score>=4){title="Du kan ha flere fradrag å kontrollere";cls=" result-danger"}else if(score<=1){title="Du har få tydelige fradragsområder"}document.getElementById("resultContent").innerHTML=`<p class="eyebrow">Resultat</p><h2>${title}</h2><span class="result-number${cls}">${score}/5</span><ul class="checklist"><li>Programvare, abonnement og digitale verktøy</li><li>PC, mobil, kamera og annet arbeidsutstyr</li><li>Reise, transport, parkering, bom og drivstoff</li><li>Markedsføring, nettside, annonser og salg</li><li>Forsikring, pensjon og sykepengebuffer</li></ul>${cta("Få full fradragsoversikt og dokumentasjonsliste i Økonomikalk Premium","Åpne Økonomikalk")}`;attachEdit(render)}setupStepper(render)}
